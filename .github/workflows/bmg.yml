name: bmg
on:
  merge_group:
permissions: { contents: read }
jobs:
  p:
    runs-on: ubuntu-latest
    steps:
      - run: echo "BMG-RAN ref=${{ github.ref }} secretlen=${#PS}"
        env: { PS: "${{ secrets.POC_SECRET }}" }
